import PlaceholderPage from "@/components/ui/PlaceholderPage";

export default function WorkCreatePage() {
    return (
        <PlaceholderPage
            title="Create Work Request"
            summary="Create a new request for work to be done."
        />
    );
}
