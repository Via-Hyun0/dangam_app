import PlaceholderPage from "@/components/ui/PlaceholderPage";

export default function ServiceRegisterPage() {
    return (
        <PlaceholderPage
            title="Register Service"
            summary="Register a new service that you can offer to others."
        />
    );
}
