import PlaceholderPage from "@/components/ui/PlaceholderPage";

export default function WorkMapPage() {
    return (
        <PlaceholderPage
            title="Work Map"
            summary="View work requests on an interactive map."
        />
    );
}
